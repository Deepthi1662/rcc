ServiceActivationDetail pbService = ServiceActivationDetail.builder()
                .serviceType(PHONE_BANKING)
                .isActive(serviceActivationStatus.getPhnBankingRegistrationFlag())
                .build();
        ServiceActivationDetail mbService = ServiceActivationDetail.builder()
                .serviceType(MOBILE_BANKING)
                .isActive(serviceActivationStatus.getMbBankngRegistrationFlag())
                .activeStatusInDays(serviceActivationStatus.getMbBankingLastActive())
                .build();
        ServiceActivationDetail nbService = ServiceActivationDetail.builder()
                .serviceType(NET_BANKING)
                .isActive(serviceActivationStatus.getNetBankingRegistrationFlag())
                .activeStatusInDays(serviceActivationStatus.getNetBankingLastActive())
                .build();
        ServiceActivationDetail posService = ServiceActivationDetail.builder()
                .serviceType(POS)
                .isActive(serviceActivationStatus.getPosRegistrationFlag())
                .activeStatusInDays(serviceActivationStatus.getPosLastActive())
                .build();

        List<ServiceActivationDetail> serviceActivationDetails = new ArrayList<>();
        serviceActivationDetails.add(pbService);
        serviceActivationDetails.add(mbService);
        serviceActivationDetails.add(nbService);
        serviceActivationDetails.add(posService);

        customer360ResponseBuilder.serviceActivationDetails(serviceActivationDetails);
